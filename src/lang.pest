WHITESPACE = _{ " " }
alpha = @{('A'..'Z'|'a'..'z')+}
number = @{ ASCII_DIGIT+ }
cell = ${ alpha ~ number }

div = { "/" }
mul = { "*" }
add = { "+" }
sub = { "-" }
op = { div | mul | add | sub }

ident = _{ SOI ~ "=" ~ cell ~ op ~  cell ~ EOI }
